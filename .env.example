# Backend URL used by Next.js rewrites (server-side only)
BACKEND_URL=http://localhost:8000

# Optional: override client-side API base (defaults to /api proxy)
# NEXT_PUBLIC_API_URL=http://localhost:8000
